services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: board_portal
      POSTGRES_USER: board_portal
      POSTGRES_PASSWORD: board_portal
    ports:
      - "5433:5432"
    volumes:
      - board_portal_pgdata:/var/lib/postgresql/data

volumes:
  board_portal_pgdata:
